extend type Query {
  twitchStreams: TwitchStreamResults
}

extend type Subscription {
  test: String!
}

type TwitchStreamResults {
  items: [TwitchStream!]
  totalCount: Int!
}

type TwitchStream {
  streamer: Streamer!
}

type Streamer {
  name: String!
}